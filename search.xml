<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>线性回归与最小二乘</title>
    <url>/2020/04/11/%E7%BA%BF%E6%80%A7%E5%9B%9E%E5%BD%92%E4%B8%8E%E6%9C%80%E5%B0%8F%E4%BA%8C%E4%B9%98/</url>
    <content><![CDATA[<h1 id="线性回归与最小二乘"><a href="#线性回归与最小二乘" class="headerlink" title="线性回归与最小二乘"></a>线性回归与最小二乘</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>线性回归（Linear Regression）是一个简单易用且十分强大的工具。</p>
<a id="more"></a>
<h2 id="线性回归"><a href="#线性回归" class="headerlink" title="线性回归"></a>线性回归</h2><p>在一些情况下，我们的数据呈现出线性，这时我们通常会尝试用形似</p>
<script type="math/tex; mode=display">
y = a x +b \tag{1}</script><p>的线性方程来做拟合、预测。上式还可以被推广至多维，即</p>
<script type="math/tex; mode=display">
y = a_1 x_1 + a_2 x_2 + \cdots + a_n x_n + b \tag{2}</script><p>上述两个方程在实际情况中为预测方程。以最简单的情况为例，更合适的写法应为</p>
<script type="math/tex; mode=display">
\hat y = a x + b</script><p>还以最简单的情况即仅有一个自变量为例，我们希望拟合得到的误差尽可能小。用一条直线来拟合数据，可以想象为用某种规律来归纳数据集中所蕴含的信息，这种做法尝试提取出数据集主要的信息规律，相应地，必将带来信息损失。为此，我们引入常用的均方差（MSE，Mean Error Square）来作为我们的损失函数，定义如下：</p>
<script type="math/tex; mode=display">
J(a, b) = \frac{1}{m}\sum_{i=1}^m(y_i - \hat y_i)^2 = \frac{1}{m}\sum_{i=1}^m[y_i - (ax_i + b)]^2\tag{3}</script><p>需要指出的是，$\{(x_i, y_i)| i=1, \dots, m\}$ 为我们的数据集，是已知量。因此，成本函数应是关于参数 $a, b$ 的函数。参数 $a, b$ 确定了最终拟合出的直线，因此它们应是使得MSE最小所对应的参数，即</p>
<script type="math/tex; mode=display">
a, b = \mathop{\arg\min}_{a, b} J(a, b) \tag{4}</script><h2 id="最小二乘"><a href="#最小二乘" class="headerlink" title="最小二乘"></a>最小二乘</h2><p>最小化MSE是一个优化问题，我们需要思考如何才能得到这样一组符合要求的参数。在上述情况下，成本函数是关于参数 $a, b$ 的二元函数，自然地，容易想到将函数对各变量求偏导，并使得偏导为0。即：</p>
<script type="math/tex; mode=display">
\left\{
\begin{aligned}
& \frac{\partial J}{\partial a}  = 2\sum_{i=1}^m(ax_i + b - y_i)  = 0 \\
& \frac{\partial J}{\partial b}  = 2\sum_{i=1}^m(ax_i + b - y_i)x_i  = 0\\
\end{aligned}
\right. \tag{5}</script><p>将上式进一步化简得</p>
<script type="math/tex; mode=display">
\left\{
\begin{aligned}
& a\sum_{i=1}^mx_i + mb - \sum_{i=1}^my_i = 0 \\
& a\sum_{i=1}^mx_i^2 + b\sum_{i=1}^mx_i - \sum_{i=1}^mx_iy_i = 0 \\
\end{aligned}
\right. \tag{6}</script><p>将 $(6)$ 式中第一个式子带入第二个式子，消去参数 $b$ ，可计算出 $a$ ；将第一个式子左右同时除以 $m$ ，可用已计算得到的 $a$ 来计算出 $b$ 。</p>
<script type="math/tex; mode=display">
\left\{
\begin{aligned}
& a = \frac{m\sum x_iy_i - \sum x_i \sum y_i}{m \sum x_i^2 - (\sum x_i)^2} = \frac{\sum x_iy_i - m\bar x \bar y}{\sum x_i^2 - m\bar x^2} \\
& b = \bar y - a \bar x \\
\end{aligned}
\right. \tag{7}</script><p>这就是我们通常所说的最小二乘。然而，现实中的数据往往拥有不止一个自变量，此时上面推导出的式子便已不能使用。下面我们结合线性代数相关知识，给出多元线性回归最小二乘的推导。</p>
<p>假设我们的数据集拥有 $n$ 个变量，包含 $m$ 个数据。数据集形似</p>
<script type="math/tex; mode=display">
x = 
\left [
\begin{matrix}
x_1^{(1)} & x_1^{(2)} & \cdots & x_1^{(n)}\\
 x_2^{(1)} & x_2^{(2)} & \cdots & x_2^{(n)}\\
 \vdots & \vdots & \ddots & \vdots \\
 x_m^{(1)} & x_m^{(2)} & \cdots & x_m^{(n)}\\
\end{matrix}
\right ]_{m \times n} \tag{8}</script><p>数据集 $x$ 中每一列代表一个变量，每一行代表一个数据。矩阵 $x$ 可被进一步表示为：</p>
<script type="math/tex; mode=display">
x = [x^{(1)}, x^{(2)}, \cdots, x^{(n)}] = [x_1, x_2, \cdots, x_m]^T \tag{9} \\</script><p>其中</p>
<script type="math/tex; mode=display">
x^{(i)} = 
\left [
\begin{matrix}
x_1^{(i)} \\
 x_2^{(i)} \\
 \vdots \\
 x_m^{(i)} \\
\end{matrix}
\right ]
, \  \ 

x_i = 
\left [
\begin{matrix}
x_i^{(1)} \\
 x_i^{(2)} \\
 \vdots \\
 x_i^{(n)} \\
\end{matrix}
\right ]. \tag{10}</script><p>回顾 $(2)$ 式的形式，采取另外的符号做等价替代：</p>
<script type="math/tex; mode=display">
\hat y_i = \theta_0 + \theta_1 x_i^{(1)} + \theta_2 x_i^{(2)} + \cdots + \theta_n x_i^{(n)} \tag{11}</script><p>若取</p>
<script type="math/tex; mode=display">
\theta = \left [
\begin{matrix}
\theta_1 \\
\theta_2 \\
\vdots \\
\theta_n \\
\end{matrix}
\right ], \tag{12}</script><p>则有</p>
<script type="math/tex; mode=display">
\hat y_i = \theta^T x_i + \theta_0 \tag{13}</script><p>这里，我们引入 $x_0$ 并使其恒为1，同时将参数 $\theta_0$ 并入参数向量，即取</p>
<script type="math/tex; mode=display">
\begin{aligned}
& \hat x_i = \left [
\begin{matrix}
x_0 \\
x_i 
\end{matrix}
\right ] = \left [
\begin{matrix}
1 \\
x_i 
\end{matrix}
\right ], \\

& \Theta = \left [
\begin{matrix}
\theta_0 \\
\theta
\end{matrix}
\right ],
\end{aligned} \tag{14}</script><p>则 $(11)$ 式可被进一步表示为</p>
<script type="math/tex; mode=display">
\hat y_i = \Theta^T \hat x_i \tag{15}</script><p>为推导起见，我们将数据集矩阵 $x$ 的最左侧添加一列元素全部为1的向量，并用符号表示出预测值组成的向量，如下：</p>
<script type="math/tex; mode=display">
\begin{aligned}
& X  = [1, x]_{m \times (n + 1)}, \\

& Y = \left [
\begin{matrix}
y_1 \\
 y_2 \\
 \vdots \\
 y_m
\end{matrix}
\right ]_{m \times 1}.

\end{aligned}
 \tag{16}</script><p>这样，我们便可以矩阵的形式来表示出损失函数：</p>
<script type="math/tex; mode=display">
\begin{aligned} 
J(\Theta) = \frac{1}{m} \sum_{i=1}^m {(y_i - \hat y_i)}^2
& = \frac{1}{m} \sum_{i=1}^m {(y_i - \Theta^T \hat x_i)}^2 \\ 
& = \frac{1}{m} \|Y - X \Theta \|_2^{2} \\ 
& = \frac{1}{m} (Y -X \Theta)^{T} (Y - X \Theta) \\ 
& = \frac{1}{m} (Y^{T} - \Theta^{T} X^{T}) (Y - X \Theta) \\ 
& = \frac{1}{m}(Y^{T} Y - Y^{T} X \Theta - \Theta^{T} X^{T} Y + \Theta^{T} X^{T} X \Theta) 
\end{aligned}</script><p>将成本函数 $J(\Theta)$ 对 $\Theta$ 求导并取0，有</p>
<script type="math/tex; mode=display">
\begin{aligned}
\frac{d J(\Theta)}{d \Theta}
= \frac{1}{m}(-X^{T}Y - X^{T}Y + 2X^{T} X \Theta)
= 0
\end{aligned}</script><p>即</p>
<script type="math/tex; mode=display">
X^{T} X \Theta = X^{T} Y \tag{17}</script><p>如果 $X^{T} X$ 可逆，则有</p>
<script type="math/tex; mode=display">
\Theta = (X^{T} X)^{-1} X^{T} Y \tag{18}</script><p>到此为止，我们已经推出了多元线性回归的参数表达式。</p>
<p>细心的读者可能会发现，上述求偏导的做法不一定能找到函数的最小值。或者说，如果目标函数是非凸的，求偏导的做法仅能得到一个局部最小值或鞍点，不能保证得到全局最优值。举个简单的例子，对于函数 $z = x^2 - y^2$ ，求偏导的做法会使你得到点 $(0, 0)$ 。但当 $x = 0$，$y$ 取任意正值时，目标函数值均要更小，点 $(0, 0)$ 只是原函数的一个鞍点。实际上，这个函数没有最小值，其取值范围是 $\mathbb{R}$ 。</p>
<p>所幸，我们的损失函数是凸函数，采用上述推导得到的结果是正确的。但也还有着其他问题存在：如果数据集的维度较大，矩阵求逆等运算可能会变得十分消耗算力，套用推导出的参数表达式可能无法满足需求；如果 $X^TX$ 不可逆，推导式便直接失效。结合上面提到的全局最优问题，后面便衍生出了诸如rmsprop、Adam等优化算法及其他措施来解决对应的问题。</p>
<h2 id="编程实现"><a href="#编程实现" class="headerlink" title="编程实现"></a>编程实现</h2><p>下面我们用Python来做简单实践。</p>
<h3 id="code-1"><a href="#code-1" class="headerlink" title="code 1"></a>code 1</h3><p>下面这段代码给出了如何用sklearn做线性回归，并查看其回归参数和评估指标MSE、R$^2$ 。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- coding = UTF-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_squared_error, r2_score</span><br><span class="line"></span><br><span class="line">X = [[<span class="number">10</span>], [<span class="number">10</span>], [<span class="number">11</span>], [<span class="number">12</span>], [<span class="number">13</span>], [<span class="number">14</span>], [<span class="number">15</span>], [<span class="number">16</span>], [<span class="number">17</span>], [<span class="number">18</span>], [<span class="number">19</span>], [<span class="number">20</span>], [<span class="number">21</span>], [<span class="number">22</span>], [<span class="number">23</span>], [<span class="number">24</span>], [<span class="number">25</span>], [<span class="number">26</span>], [<span class="number">27</span>], [<span class="number">28</span>]]</span><br><span class="line">Y = [[<span class="number">7.7</span>], [<span class="number">9.87</span>], [<span class="number">11.18</span>], [<span class="number">10.43</span>], [<span class="number">12.36</span>], [<span class="number">14.15</span>], [<span class="number">15.73</span>], [<span class="number">16.4</span>], [<span class="number">18.86</span>], [<span class="number">16.13</span>], [<span class="number">18.21</span>], [<span class="number">18.37</span>], [<span class="number">22.61</span>], [<span class="number">19.83</span>], [<span class="number">22.67</span>], [<span class="number">22.7</span>], [<span class="number">25.16</span>], [<span class="number">25.55</span>], [<span class="number">28.21</span>], [<span class="number">28.12</span>]]</span><br><span class="line"></span><br><span class="line">model = LinearRegression()</span><br><span class="line">model.fit(X, Y)</span><br><span class="line">a = float(model.coef_) <span class="comment">#线性模型的系数</span></span><br><span class="line">b = float(model.intercept_) <span class="comment">#截距</span></span><br><span class="line">print(<span class="string">'a = &#123;&#125;, b = &#123;&#125;\n'</span>.format(a, b))</span><br><span class="line"></span><br><span class="line">f = <span class="keyword">lambda</span> x: a * x + b</span><br><span class="line">predict = [f(k[<span class="number">0</span>]) <span class="keyword">for</span> k <span class="keyword">in</span> X]</span><br><span class="line"></span><br><span class="line">R2 = r2_score(Y, predict)</span><br><span class="line">MSE = mean_squared_error(Y, predict)</span><br><span class="line">print(<span class="string">'R2 = &#123;&#125; \n MSE = &#123;&#125;'</span>.format(R2, MSE))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">plt.scatter(X, Y, color=<span class="string">'blue'</span>)</span><br><span class="line">plt.plot([i <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">9</span>, <span class="number">31</span>, <span class="number">2</span>)], [a*i+b <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">9</span>, <span class="number">31</span>, <span class="number">2</span>)], color=<span class="string">'red'</span>)</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<h3 id="code-2"><a href="#code-2" class="headerlink" title="code 2"></a>code 2</h3><p>下面这段代码中使用了推导式来计算参数，并将其与使用sklearn得到的参数进行了对比。事实证明这两种操作得到的结果完全一样。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- coding = UTF-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> numpy.linalg <span class="keyword">import</span> inv</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression</span><br><span class="line"></span><br><span class="line">x = [<span class="number">0.03</span>, <span class="number">0.04</span>, <span class="number">0.05</span>, <span class="number">0.07</span>, <span class="number">0.09</span>, <span class="number">0.1</span>, <span class="number">0.12</span>, <span class="number">0.15</span>, <span class="number">0.17</span>,   <span class="number">0.2</span>]</span><br><span class="line">y = [<span class="number">40.5</span>, <span class="number">39.5</span>, <span class="number">41</span>, <span class="number">41.5</span>, <span class="number">43</span>, <span class="number">42</span>, <span class="number">45</span>, <span class="number">47.5</span>, <span class="number">53</span>, <span class="number">56</span>]</span><br><span class="line"></span><br><span class="line"><span class="string">'''首先采用推导式求系数做线性回归：'''</span></span><br><span class="line"></span><br><span class="line">X = np.ones((<span class="number">10</span>, <span class="number">2</span>))</span><br><span class="line">Y = np.ones((<span class="number">10</span>, <span class="number">1</span>))</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>):</span><br><span class="line">	X[i, <span class="number">1</span>] = x[i]</span><br><span class="line">	Y[i, <span class="number">0</span>] = y[i]</span><br><span class="line">B = inv(X.transpose().dot(X)).dot(X.transpose()).dot(Y)</span><br><span class="line">print(<span class="string">'B = &#123;&#125;\n'</span>.format(B)) <span class="comment"># 回归参数组成的向量</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="string">'''下面采用sklearn方法做线性回归：'''</span></span><br><span class="line"></span><br><span class="line">f = <span class="keyword">lambda</span> x: round(x, <span class="number">8</span>)</span><br><span class="line"></span><br><span class="line">X = np.ones((<span class="number">10</span>, <span class="number">1</span>))</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>):</span><br><span class="line">	X[i, <span class="number">0</span>] = x[i]</span><br><span class="line"></span><br><span class="line">model = LinearRegression()</span><br><span class="line">model.fit(X, Y)</span><br><span class="line">theta_0 = float(model.intercept_)</span><br><span class="line">theta_1 = float(model.coef_)</span><br><span class="line">print(theta_0, <span class="string">'\n'</span>, theta_1)</span><br><span class="line"><span class="comment"># 由于浮点数精度的原因，两个小数不能直接做比较，</span></span><br><span class="line"><span class="comment"># 将不同方法计算得到的参数取8位小数后再做比较</span></span><br><span class="line">print(f(theta_0) == f(B[<span class="number">0</span>, <span class="number">0</span>]))</span><br><span class="line">print(f(theta_1) == f(B[<span class="number">1</span>, <span class="number">0</span>]))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">plt.plot([<span class="number">0.02</span>, <span class="number">0.22</span>], B[<span class="number">0</span>, <span class="number">0</span>] + B[<span class="number">1</span>, <span class="number">0</span>] * np.array([<span class="number">0.02</span>, <span class="number">0.22</span>]), color=<span class="string">'red'</span>)</span><br><span class="line">plt.scatter(x, y)</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p>为方便代码运行，这里将实例数据直接放入了代码中。还可以尝试sklearn中的make_regression函数来生成回归数据。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_regression</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>算法与模型</category>
      </categories>
      <tags>
        <tag>线性回归</tag>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>lasso回归与岭回归</title>
    <url>/2020/04/16/lasso%E5%9B%9E%E5%BD%92%E4%B8%8E%E5%B2%AD%E5%9B%9E%E5%BD%92/</url>
    <content><![CDATA[<h1 id="lasso回归与岭回归"><a href="#lasso回归与岭回归" class="headerlink" title="lasso回归与岭回归"></a>lasso回归与岭回归</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>lasso回归（Least Absolute Shrinkage and Selection Operator Regression）与岭回归（Ridge Regression）以线性回归为基础，在其之上做出了一定的改进。</p>
<a id="more"></a>
<h2 id="Preliminaries-and-Notation：线性回归"><a href="#Preliminaries-and-Notation：线性回归" class="headerlink" title="Preliminaries and Notation：线性回归"></a>Preliminaries and Notation：线性回归</h2><p>对于一个线性回归模型，我们有：</p>
<p>假设数据集中共有 $n$ 个变量，包含 $m$ 个数据。数据集形似</p>
<script type="math/tex; mode=display">
x = 
\left [
\begin{matrix}
x_1^{(1)} & x_1^{(2)} & \cdots & x_1^{(n)}\\
 x_2^{(1)} & x_2^{(2)} & \cdots & x_2^{(n)}\\
 \vdots & \vdots & \ddots & \vdots \\
 x_m^{(1)} & x_m^{(2)} & \cdots & x_m^{(n)}\\
\end{matrix}
\right ]_{m \times n}
\tag{1}</script><p>数据集 $x$ 中每一列代表一个变量，每一行代表一个数据。矩阵 $x$ 可被进一步表示为：</p>
<script type="math/tex; mode=display">
x = [x^{(1)}, x^{(2)}, \cdots, x^{(n)}] = [x_1, x_2, \cdots, x_m]^T \\
\tag{2}</script><p>其中</p>
<script type="math/tex; mode=display">
x^{(i)} = 
\left [
\begin{matrix}
x_1^{(i)} \\
 x_2^{(i)} \\
 \vdots \\
 x_m^{(i)} \\
\end{matrix}
\right ]
, \  \ 

x_i = 
\left [
\begin{matrix}
x_i^{(1)} \\
 x_i^{(2)} \\
 \vdots \\
 x_i^{(n)} \\
\end{matrix}
\right ].
\tag{3}</script><p>并取</p>
<script type="math/tex; mode=display">
X  = [1, x] = \left [\begin{matrix}1 & x_1^{(1)} & \cdots & x_1^{(n)}\\ 1 & x_2^{(1)} & \cdots & x_2^{(n)}\\ \vdots & \vdots & \ddots & \vdots \\ 1 & x_m^{(1)} & \cdots & x_m^{(n)}\\\end{matrix} \right ]_{m \times (n+1)}, \hat x_i = \left [\begin{matrix}x_0 \\x_i \end{matrix}\right].
\tag{4}</script><p>已知每个数据对应的真实值，并用 $\hat .$ 来表示对应的预测值或估计值：</p>
<script type="math/tex; mode=display">
\begin{aligned}
& Y = \left [
\begin{matrix}
y_1 \\
 y_2 \\
 \vdots \\
 y_m
\end{matrix}
\right ], 

\end{aligned}

\hat Y = \left [
\begin{matrix}
\hat y_1 \\
 \hat y_2 \\
 \vdots \\
 \hat y_m
\end{matrix}
\right ]. \tag{5}</script><p>取系数向量为</p>
<script type="math/tex; mode=display">
\beta = \left [
\begin{matrix}
\beta_0 \\
\beta_1 \\
\vdots \\
\beta_n \\
\end{matrix}
\right ]
=
\left [
\begin{matrix}
1 \\
\beta_1 \\
\vdots \\
\beta_n \\
\end{matrix}
\right ]
. \tag{6}</script><p>则我们对应的线性模型即为</p>
<script type="math/tex; mode=display">
\begin{aligned}
& \hat y_i = \hat x_i\beta, \\
& \hat Y = X\beta
\end{aligned}
\tag{7}</script><p>对于线性回归，其目标（成本）函数为</p>
<script type="math/tex; mode=display">
J(\beta) = \frac{1}{m}\sum_{i=1}^m(y_i - \hat y_i)^2 = \frac{1}{m}\sum_{i=1}^m(y_i - \hat x_i \beta)^2 = \frac{1}{m}||Y-X\beta||_2^2
\tag{8}</script><p>对应最优的系数估计 $\hat \beta$ 即为</p>
<script type="math/tex; mode=display">
\hat \beta = \mathop{\arg\min}_{\beta} J(\beta) = \mathop{\arg\min}_{\beta} \frac{1}{m}||Y-X\beta||_2^2
\tag{9}</script><p>我们之前已经推导过，线性回归最优的 $\beta$ 为：</p>
<script type="math/tex; mode=display">
\hat \beta = (X^TX)^{-1}X^TY \tag{10}</script><h2 id="LASSO回归与岭回归"><a href="#LASSO回归与岭回归" class="headerlink" title="LASSO回归与岭回归"></a>LASSO回归与岭回归</h2><p>lasso回归与岭回归建立在原线性回归的基础上。为了防止系数向量中某些系数很大，而另一些系数又很小，我们希望在成本函数中加入系数的惩罚项，即将成本函数做出以下变化：</p>
<script type="math/tex; mode=display">
\begin{aligned}
& C_1(\beta) = J(\beta) + \lambda ||\beta||_1 = \frac{1}{m}||Y-X\beta||_2^2 + \lambda||\beta||_1, \\

& C_2(\beta) = J(\beta) + \lambda ||\beta||_2 = \frac{1}{m}||Y-X\beta||_2^2 + \lambda||\beta||_2
\end{aligned}
\tag{11}</script><p>其中，$\lambda &gt;0$； $||\beta||_1, ||\beta||_2$ 分别为系数向量 $\beta$ 的L1范数与L2范数，具体为</p>
<script type="math/tex; mode=display">
\begin{aligned}
& ||\beta||_1 = \sum_{i=0}^m |\beta_i|, \\

& ||\beta||_2 = \sum_{i=0}^m \beta_i^2
\end{aligned}
\tag{12}</script><p>如果在成本函数中加入的是L1范数，则其对应的模型即为<strong>LASSO回归</strong>；如果在成本函数中加入的是L2范数，则其对应的模型即为<strong>岭回归</strong>。</p>
<p>我们一方面想要使得拟合出的直线有着最小的拟合误差，同时又希望拟合得到的系数不能太大，通过将两部分整合至一个成本函数中，可以实现这样一个目标。可以注意到，成本函数中，第一部分代表着拟合误差，第二部分代表着系数向量的大小，而超参数 $\lambda$ 作为它们的桥梁，平衡两者在成本函数中各自的权重。</p>
<p>在之前推导线性回归的最优系数解时，我们采用了对其目标函数求导取极值的方式，这样做的前提是目标函数连续可微，且应当是（下）凸函数。显而易见，成本函数 $C_1(\beta)$ 并不满足这一性质，因为绝对值项是连续、不可导的；而 $C_2(\beta)$ 是满足的。因此，我们可以采用同样的思路去求解岭回归，但对于LASSO回归，我们需要采用其他的方法求解。本次仅给出岭回归的求解。</p>
<h2 id="岭回归的求解"><a href="#岭回归的求解" class="headerlink" title="岭回归的求解"></a>岭回归的求解</h2><p>岭回归对应的目标（成本）函数为</p>
<script type="math/tex; mode=display">
\begin{aligned} 
C_2(\beta) = \frac{1}{m}||Y-X\beta||_2^2 + \lambda||\beta||_2
& = \frac{1}{m} (Y -X \beta)^{T} (Y - X \beta) + \lambda\beta^T\beta\\ 
& = \frac{1}{m} (Y^{T} - \beta^{T} X^{T}) (Y - X \beta)  + \lambda\beta^T\beta\\ 
& = \frac{1}{m}(Y^{T} Y - Y^{T} X \beta - \beta^{T} X^{T} Y + \beta^{T} X^{T} X \beta)  + \lambda\beta^T\beta
\end{aligned}
\tag{13}</script><p>将成本函数 $C_2(\beta)$ 对 $\beta$ 求导并取0，有</p>
<script type="math/tex; mode=display">
\frac{\rm d C_2}{\rm d \beta}=2 X^{T} X \beta-2 X^{T} Y+2 \lambda I \beta=0 \tag{14}</script><p>即</p>
<script type="math/tex; mode=display">
\left(X^{T} X+\lambda I\right) \beta=X^{T} Y \tag{15}</script><p>解析解即为</p>
<script type="math/tex; mode=display">
\hat \beta = (X^TX + \lambda I)^{-1}X^TY \tag{16}</script><p>这里，需要指出的是，<strong>矩阵 $(X^TX + \lambda I)$ 一定可逆</strong>，这就避免了线性回归最优解 $(10)$ 的潜在问题：$X^TX$ 可能不可逆。下面给出证明。</p>
<blockquote>
<blockquote>
<p>$\mathbf{Lemma \ 1:}$  对任意实矩阵 $D \in \mathbb R^{m\times n}$ ， $D^TD$ 是半正定的。</p>
<p>$\mathbf{Proof^*}$ 任取实向量 $x \in \mathbb R^n$ ，有 $x^T(D^TD)x = (Dx)^TDx = ||Dx||_2^2 \ge 0$ .</p>
<p>$\mathbf{Lemma \ 2:}$ 如果矩阵 $M \in \mathbb R^{n\times n}$ 是半正定的，则其特征值非负。</p>
<p>$\mathbf{Lemma \ 3:}$ 对于齐次线性方程 $Ax = 0$ ，若方程只有零解，则矩阵 $A$ 可逆；如果方程有无穷解，则矩阵 $A$ 不可逆。</p>
</blockquote>
<p>$\mathbf{Proof:}$</p>
<p>构造以下方程组：</p>
<script type="math/tex; mode=display">
(X^TX + \lambda I) x = 0 \tag{17}</script><p>移项，可得</p>
<script type="math/tex; mode=display">
X^TX x = -\lambda x \tag{18}</script><p>根据特征向量的定义，可知 $x$ 即为矩阵 $X^TX$ 对应于特征值 $-\lambda$ 的特征向量。又因为矩阵 $X^TX$ 是半定的，上面的引理1、2指出其特征值非负；而 $-\lambda &lt; 0$ ，故 $x = 0$ 。</p>
<p>因此，方程组 $(17)$ 仅有零解，由引理3可知矩阵 $(X^TX + \lambda I)$ 可逆。<strong>Q.E.D.</strong></p>
</blockquote>
<h2 id="编程实现"><a href="#编程实现" class="headerlink" title="编程实现"></a>编程实现</h2><p>下面我们用Python来做简单实现。</p>
<p>首先使用<code>sklearn.datasets.make_regression</code>函数来生成我们的数据集。我们生成了1000个数据，自变量与因变量的个数均为1，真实截距设置为2，噪音系数设置为75。</p>
<p>最后将lasso回归、岭回归与线性回归放在一起作对比。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- coding = UTF-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_regression</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression, Lasso, Ridge</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> r2_score, mean_squared_error</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">x, y, coef = make_regression(n_samples=<span class="number">1000</span>, n_features=<span class="number">1</span>, n_targets=<span class="number">1</span>, coef=<span class="literal">True</span>, bias=<span class="number">2</span>, noise=<span class="number">75</span>, shuffle=<span class="literal">True</span>, random_state=<span class="number">13</span>)</span><br><span class="line"></span><br><span class="line">model = LinearRegression()</span><br><span class="line">model_1 = Lasso(alpha=<span class="number">20</span>, random_state=<span class="number">13</span>)</span><br><span class="line">model_2 = Ridge(alpha=<span class="number">20</span>, random_state=<span class="number">13</span>)</span><br><span class="line"></span><br><span class="line">model.fit(x, y)</span><br><span class="line">model_1.fit(x, y)</span><br><span class="line">model_2.fit(x, y)</span><br><span class="line"></span><br><span class="line">print(<span class="string">'Linear Regression R^2:   &#123;:.6f&#125;'</span>, r2_score(model.predict(x), y))</span><br><span class="line">print(<span class="string">'LASSO Regression R^2:   &#123;:.6f&#125;'</span>, r2_score(model_1.predict(x), y))</span><br><span class="line">print(<span class="string">'Ridge Regression R^2:   &#123;:.6f&#125;'</span>, r2_score(model_2.predict(x), y))</span><br><span class="line">print(<span class="string">'\n\n'</span>)</span><br><span class="line">print(<span class="string">'Linear Regression MSE:   &#123;:.6f&#125;'</span>, mean_squared_error(model.predict(x), y))</span><br><span class="line">print(<span class="string">'LASSO Regression MSE:   &#123;:.6f&#125;'</span>, mean_squared_error(model_1.predict(x), y))</span><br><span class="line">print(<span class="string">'Ridge Regression MSE:   &#123;:.6f&#125;'</span>, mean_squared_error(model_2.predict(x), y))</span><br><span class="line"></span><br><span class="line"><span class="comment">#print(model.coef_, model.intercept_)</span></span><br><span class="line"><span class="comment">#print(coef)</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">plt.scatter(x, y, s=<span class="number">8</span>)</span><br><span class="line">ls = np.array([i/<span class="number">100</span><span class="number">-3</span> <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">0</span>, <span class="number">600</span>)]).reshape(<span class="number">-1</span>, <span class="number">1</span>)</span><br><span class="line">plt.plot(ls, model.predict(ls), color=<span class="string">'blue'</span>, label=<span class="string">'Linear'</span>)</span><br><span class="line">plt.plot(ls, model_1.predict(ls), color=<span class="string">'red'</span>, label=<span class="string">'LASSO'</span>)</span><br><span class="line">plt.plot(ls, model_2.predict(ls), color=<span class="string">'green'</span>, label=<span class="string">'Ridge'</span>)</span><br><span class="line">plt.legend()</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p><img src="/images/lasso回归与岭回归/image1.png" alt="alt"></p>
<p>可以看到，岭回归与线性回归几乎重合，而LASSO回归则呈现出与它们不一样的结果。事实上，LASSO回归所使用的L1正则有着稀疏诱导的性质，虽然L1正则与L2正则均能够起到收缩系数的作用，但L1正则的收缩效果更强，它能使得系数收缩为0，而L2正则仅能使系数收缩至一个较小的数，不能收缩为0。因此，在有着多个输入变量时，LASSO回归能够起到变量选择的作用，将其中某些变量的系数收缩至0，即使得系数向量具有稀疏性。</p>
]]></content>
      <categories>
        <category>算法与模型</category>
      </categories>
      <tags>
        <tag>线性回归</tag>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/04/10/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p>
<a id="more"></a>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>
]]></content>
  </entry>
</search>
